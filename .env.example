# AURA — copy to .env.local and fill. Never commit .env* (see .gitignore)
# Eco demo runs without these — rule fallback + mock data handle it
OPENAI_API_KEY=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_DEMO_MODE=true
